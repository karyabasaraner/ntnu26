{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build current test",
            "type": "shell",
            "command": "cmake --build ${workspaceFolder}/build --target ${fileBasenameNoExtension}",
            "group": "build",
            "problemMatcher": ["$gcc"]
        }
    ]
}
